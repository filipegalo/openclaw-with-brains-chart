apiVersion: v1
kind: Service
metadata:
  name: {{ include "openclaw-with-brain.fullname" . }}
  labels:
    {{- include "openclaw-with-brain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "openclaw-with-brain.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
